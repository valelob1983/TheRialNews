<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <%= link_to 'TheRialNews', root_path, class: 'navbar-brand text-white' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Noticias', news_index_path, class: 'nav-link text-white' %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Comentarios', comments_path, class: 'nav-link text-white' %>
                </li>
                <li class="nav-item dropdown bg-dark">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if user_signed_in? %>
                            Hola <%= current_user.email %>
                        <% else %>
                            Regístrate o Inicia Sesión
                        <% end %>
                    </a>
                    <ul class="dropdown-menu bg-dark">
                        <% if user_signed_in? %>
                            <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item text-white', method: :delete %></li>
                        <% else %>
                            <li><%= link_to 'Iniciar sesión', new_user_session_path, class: 'dropdown-item text-white' %></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><%= link_to 'Registro', new_user_registration_path, class: 'dropdown-item text-white' %></li>
                        <% end %>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

